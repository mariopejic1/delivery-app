<%- include("../partials/header") %>

<div class="container">
    <h2>💳 Simulacija kartičnog plaćanja</h2>

    <form method="POST" action="/shipments/payment/<%= shipmentId %>" class="card-form">

        <label>Ime i prezime</label>
        <input required name="name" value="<%= locals.values ? values.name : '' %>">

        <label>Broj kartice</label>
        <input required name="cardNumber" maxlength="16" placeholder="1234 5678 9012 3456" 
               value="<%= locals.values ? values.cardNumber : '' %>">

        <label>Datum isteka</label>
        <input required name="expiry" placeholder="MM/YY" 
               value="<%= locals.values ? values.expiry : '' %>">

        <label>CVV</label>
        <input required name="cvv" maxlength="3" type="password">

        <% if (locals.error) { %>
            <div style="background: #f8d7da; color: #721c24; padding: 10px; border-radius: 5px; margin: 15px 0; border: 1px solid #f5c6cb;">
                <%= error %>
            </div>
        <% } %>

        <button type="submit" class="btn btn-primary">Plati (simulacija)</button>

    </form>

    <p style="margin-top:15px;color:gray">
        ⚠ Ovo je samo simulacija – stvarno plaćanje se NE izvršava
    </p>
</div>

<%- include("../partials/footer") %>